//
// Copyright (c) 2018 Volterra, Inc. All rights reserved.
// Code generated by ves-gen-tf-provider. DO NOT EDIT.
//

package volterra

import (
	"context"
	"fmt"
	"log"
	"strings"

	"github.com/hashicorp/terraform-plugin-sdk/v2/helper/schema"
	"gopkg.volterra.us/stdlib/client/vesapi"

	ves_io_schema "github.com/volterraedge/terraform-provider-volterra/pbgo/extschema/schema"
	ves_io_schema_shape_bot_defense_mobile_base_config "github.com/volterraedge/terraform-provider-volterra/pbgo/extschema/schema/shape/bot_defense/mobile_base_config"
)

// resourceVolterraMobileBaseConfig is implementation of Volterra's MobileBaseConfig resources
func resourceVolterraMobileBaseConfig() *schema.Resource {
	return &schema.Resource{
		Create: resourceVolterraMobileBaseConfigCreate,
		Read:   resourceVolterraMobileBaseConfigRead,
		Update: resourceVolterraMobileBaseConfigUpdate,
		Delete: resourceVolterraMobileBaseConfigDelete,

		Schema: map[string]*schema.Schema{

			"annotations": {
				Type:     schema.TypeMap,
				Optional: true,
			},

			"description": {
				Type:     schema.TypeString,
				Optional: true,
			},

			"disable": {
				Type:     schema.TypeBool,
				Optional: true,
			},

			"labels": {
				Type:     schema.TypeMap,
				Optional: true,
			},

			"name": {
				Type:     schema.TypeString,
				Required: true,
				ForceNew: true,
			},

			"namespace": {
				Type:     schema.TypeString,
				Required: true,
				ForceNew: true,
			},

			"fqdn": {
				Type:     schema.TypeString,
				Required: true,
			},

			"os": {
				Type:     schema.TypeString,
				Required: true,
			},
		},
	}
}

// resourceVolterraMobileBaseConfigCreate creates MobileBaseConfig resource
func resourceVolterraMobileBaseConfigCreate(d *schema.ResourceData, meta interface{}) error {
	client := meta.(*APIClient)

	createMeta := &ves_io_schema.ObjectCreateMetaType{}
	createSpec := &ves_io_schema_shape_bot_defense_mobile_base_config.CreateSpecType{}
	createReq := &ves_io_schema_shape_bot_defense_mobile_base_config.CreateRequest{
		Metadata: createMeta,
		Spec:     createSpec,
	}

	if v, ok := d.GetOk("annotations"); ok && !isIntfNil(v) {

		ms := map[string]string{}

		for k, v := range v.(map[string]interface{}) {
			val := v.(string)
			ms[k] = val
		}
		createMeta.Annotations = ms
	}

	if v, ok := d.GetOk("description"); ok && !isIntfNil(v) {
		createMeta.Description =
			v.(string)
	}

	if v, ok := d.GetOk("disable"); ok && !isIntfNil(v) {
		createMeta.Disable =
			v.(bool)
	}

	if v, ok := d.GetOk("labels"); ok && !isIntfNil(v) {

		ms := map[string]string{}

		for k, v := range v.(map[string]interface{}) {
			val := v.(string)
			ms[k] = val
		}
		createMeta.Labels = ms
	}

	if v, ok := d.GetOk("name"); ok && !isIntfNil(v) {
		createMeta.Name =
			v.(string)
	}

	if v, ok := d.GetOk("namespace"); ok && !isIntfNil(v) {
		createMeta.Namespace =
			v.(string)
	}

	//fqdn
	if v, ok := d.GetOk("fqdn"); ok && !isIntfNil(v) {

		createSpec.Fqdn =
			v.(string)

	}

	//os
	if v, ok := d.GetOk("os"); ok && !isIntfNil(v) {

		createSpec.Os = ves_io_schema_shape_bot_defense_mobile_base_config.MobileOS(ves_io_schema_shape_bot_defense_mobile_base_config.MobileOS_value[v.(string)])

	}

	log.Printf("[DEBUG] Creating Volterra MobileBaseConfig object with struct: %+v", createReq)

	createMobileBaseConfigResp, err := client.CreateObject(context.Background(), ves_io_schema_shape_bot_defense_mobile_base_config.ObjectType, createReq)
	if err != nil {
		return fmt.Errorf("error creating MobileBaseConfig: %s", err)
	}
	d.SetId(createMobileBaseConfigResp.GetObjSystemMetadata().GetUid())

	return resourceVolterraMobileBaseConfigRead(d, meta)
}

func resourceVolterraMobileBaseConfigRead(d *schema.ResourceData, meta interface{}) error {
	client := meta.(*APIClient)
	name := d.Get("name").(string)
	namespace := d.Get("namespace").(string)

	resp, err := client.GetObject(context.Background(), ves_io_schema_shape_bot_defense_mobile_base_config.ObjectType, namespace, name)
	if err != nil {
		if strings.Contains(err.Error(), "status code 404") {
			log.Printf("[INFO] MobileBaseConfig %s no longer exists", d.Id())
			d.SetId("")
			return nil
		}
		return fmt.Errorf("Error finding Volterra MobileBaseConfig %q: %s", d.Id(), err)
	}
	return setMobileBaseConfigFields(client, d, resp)
}

func setMobileBaseConfigFields(client *APIClient, d *schema.ResourceData, resp vesapi.GetObjectResponse) error {
	metadata := resp.GetObjMetadata()

	d.Set("annotations", metadata.GetAnnotations())

	d.Set("description", metadata.GetDescription())

	d.Set("disable", metadata.GetDisable())

	d.Set("labels", metadata.GetLabels())

	d.Set("name", metadata.GetName())

	d.Set("namespace", metadata.GetNamespace())

	return nil
}

// resourceVolterraMobileBaseConfigUpdate updates MobileBaseConfig resource
func resourceVolterraMobileBaseConfigUpdate(d *schema.ResourceData, meta interface{}) error {
	client := meta.(*APIClient)

	updateMeta := &ves_io_schema.ObjectReplaceMetaType{}
	updateSpec := &ves_io_schema_shape_bot_defense_mobile_base_config.ReplaceSpecType{}
	updateReq := &ves_io_schema_shape_bot_defense_mobile_base_config.ReplaceRequest{
		Metadata: updateMeta,
		Spec:     updateSpec,
	}

	if v, ok := d.GetOk("annotations"); ok && !isIntfNil(v) {

		ms := map[string]string{}

		for k, v := range v.(map[string]interface{}) {
			val := v.(string)
			ms[k] = val
		}
		updateMeta.Annotations = ms
	}

	if v, ok := d.GetOk("description"); ok && !isIntfNil(v) {
		updateMeta.Description =
			v.(string)
	}

	if v, ok := d.GetOk("disable"); ok && !isIntfNil(v) {
		updateMeta.Disable =
			v.(bool)
	}

	if v, ok := d.GetOk("labels"); ok && !isIntfNil(v) {

		ms := map[string]string{}

		for k, v := range v.(map[string]interface{}) {
			val := v.(string)
			ms[k] = val
		}
		updateMeta.Labels = ms
	}

	if v, ok := d.GetOk("name"); ok && !isIntfNil(v) {
		updateMeta.Name =
			v.(string)
	}

	if v, ok := d.GetOk("namespace"); ok && !isIntfNil(v) {
		updateMeta.Namespace =
			v.(string)
	}

	if v, ok := d.GetOk("fqdn"); ok && !isIntfNil(v) {

		updateSpec.Fqdn =
			v.(string)

	}

	if v, ok := d.GetOk("os"); ok && !isIntfNil(v) {

		updateSpec.Os = ves_io_schema_shape_bot_defense_mobile_base_config.MobileOS(ves_io_schema_shape_bot_defense_mobile_base_config.MobileOS_value[v.(string)])

	}

	log.Printf("[DEBUG] Updating Volterra MobileBaseConfig obj with struct: %+v", updateReq)

	err := client.ReplaceObject(context.Background(), ves_io_schema_shape_bot_defense_mobile_base_config.ObjectType, updateReq)
	if err != nil {
		return fmt.Errorf("error updating MobileBaseConfig: %s", err)
	}

	return resourceVolterraMobileBaseConfigRead(d, meta)
}

func resourceVolterraMobileBaseConfigDelete(d *schema.ResourceData, meta interface{}) error {
	client := meta.(*APIClient)
	name := d.Get("name").(string)
	namespace := d.Get("namespace").(string)

	_, err := client.GetObject(context.Background(), ves_io_schema_shape_bot_defense_mobile_base_config.ObjectType, namespace, name)
	if err != nil {
		if strings.Contains(err.Error(), "status code 404") {
			log.Printf("[INFO] MobileBaseConfig %s no longer exists", d.Id())
			d.SetId("")
			return nil
		}
		return fmt.Errorf("Error finding Volterra MobileBaseConfig before deleting %q: %s", d.Id(), err)
	}

	log.Printf("[DEBUG] Deleting Volterra MobileBaseConfig obj with name %+v in namespace %+v", name, namespace)
	opts := []vesapi.CallOpt{
		vesapi.WithFailIfReferred(),
	}
	return client.DeleteObject(context.Background(), ves_io_schema_shape_bot_defense_mobile_base_config.ObjectType, namespace, name, opts...)
}
