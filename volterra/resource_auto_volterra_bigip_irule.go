//
// Copyright (c) 2018 Volterra, Inc. All rights reserved.
// Code generated by ves-gen-tf-provider. DO NOT EDIT.
//

package volterra

import (
	"context"
	"fmt"
	"log"
	"strings"

	"github.com/hashicorp/terraform-plugin-sdk/v2/helper/schema"
	"gopkg.volterra.us/stdlib/client/vesapi"

	ves_io_schema "github.com/volterraedge/terraform-provider-volterra/pbgo/extschema/schema"
	ves_io_schema_bigip_irule "github.com/volterraedge/terraform-provider-volterra/pbgo/extschema/schema/bigip_irule"
)

// resourceVolterraBigipIrule is implementation of Volterra's BigipIrule resources
func resourceVolterraBigipIrule() *schema.Resource {
	return &schema.Resource{
		Create: resourceVolterraBigipIruleCreate,
		Read:   resourceVolterraBigipIruleRead,
		Update: resourceVolterraBigipIruleUpdate,
		Delete: resourceVolterraBigipIruleDelete,

		Schema: map[string]*schema.Schema{

			"annotations": {
				Type:     schema.TypeMap,
				Optional: true,
			},

			"description": {
				Type:     schema.TypeString,
				Optional: true,
			},

			"disable": {
				Type:     schema.TypeBool,
				Optional: true,
			},

			"labels": {
				Type:     schema.TypeMap,
				Optional: true,
			},

			"name": {
				Type:     schema.TypeString,
				Required: true,
				ForceNew: true,
			},

			"namespace": {
				Type:     schema.TypeString,
				Required: true,
				ForceNew: true,
			},

			"code": {
				Type:     schema.TypeString,
				Optional: true,
			},

			"irule_name": {
				Type:     schema.TypeString,
				Optional: true,
			},

			"source": {
				Type:     schema.TypeString,
				Optional: true,
			},
		},
	}
}

// resourceVolterraBigipIruleCreate creates BigipIrule resource
func resourceVolterraBigipIruleCreate(d *schema.ResourceData, meta interface{}) error {
	client := meta.(*APIClient)

	createMeta := &ves_io_schema.ObjectCreateMetaType{}
	createSpec := &ves_io_schema_bigip_irule.CreateSpecType{}
	createReq := &ves_io_schema_bigip_irule.CreateRequest{
		Metadata: createMeta,
		Spec:     createSpec,
	}

	if v, ok := d.GetOk("annotations"); ok && !isIntfNil(v) {

		ms := map[string]string{}

		for k, v := range v.(map[string]interface{}) {
			val := v.(string)
			ms[k] = val
		}
		createMeta.Annotations = ms
	}

	if v, ok := d.GetOk("description"); ok && !isIntfNil(v) {
		createMeta.Description =
			v.(string)
	}

	if v, ok := d.GetOk("disable"); ok && !isIntfNil(v) {
		createMeta.Disable =
			v.(bool)
	}

	if v, ok := d.GetOk("labels"); ok && !isIntfNil(v) {

		ms := map[string]string{}

		for k, v := range v.(map[string]interface{}) {
			val := v.(string)
			ms[k] = val
		}
		createMeta.Labels = ms
	}

	if v, ok := d.GetOk("name"); ok && !isIntfNil(v) {
		createMeta.Name =
			v.(string)
	}

	if v, ok := d.GetOk("namespace"); ok && !isIntfNil(v) {
		createMeta.Namespace =
			v.(string)
	}

	//code
	if v, ok := d.GetOk("code"); ok && !isIntfNil(v) {

		createSpec.Code =
			v.(string)

	}

	//irule_name
	if v, ok := d.GetOk("irule_name"); ok && !isIntfNil(v) {

		createSpec.IruleName =
			v.(string)

	}

	//source
	if v, ok := d.GetOk("source"); ok && !isIntfNil(v) {

		createSpec.Source =
			v.(string)

	}

	log.Printf("[DEBUG] Creating Volterra BigipIrule object with struct: %+v", createReq)

	createBigipIruleResp, err := client.CreateObject(context.Background(), ves_io_schema_bigip_irule.ObjectType, createReq)
	if err != nil {
		return fmt.Errorf("error creating BigipIrule: %s", err)
	}
	d.SetId(createBigipIruleResp.GetObjSystemMetadata().GetUid())

	return resourceVolterraBigipIruleRead(d, meta)
}

func resourceVolterraBigipIruleRead(d *schema.ResourceData, meta interface{}) error {
	client := meta.(*APIClient)
	name := d.Get("name").(string)
	namespace := d.Get("namespace").(string)

	resp, err := client.GetObject(context.Background(), ves_io_schema_bigip_irule.ObjectType, namespace, name)
	if err != nil {
		if strings.Contains(err.Error(), "status code 404") {
			log.Printf("[INFO] BigipIrule %s no longer exists", d.Id())
			d.SetId("")
			return nil
		}
		return fmt.Errorf("Error finding Volterra BigipIrule %q: %s", d.Id(), err)
	}
	return setBigipIruleFields(client, d, resp)
}

func setBigipIruleFields(client *APIClient, d *schema.ResourceData, resp vesapi.GetObjectResponse) error {
	metadata := resp.GetObjMetadata()

	d.Set("annotations", metadata.GetAnnotations())

	d.Set("description", metadata.GetDescription())

	d.Set("disable", metadata.GetDisable())

	d.Set("labels", metadata.GetLabels())

	d.Set("name", metadata.GetName())

	d.Set("namespace", metadata.GetNamespace())

	return nil
}

// resourceVolterraBigipIruleUpdate updates BigipIrule resource
func resourceVolterraBigipIruleUpdate(d *schema.ResourceData, meta interface{}) error {
	client := meta.(*APIClient)

	updateMeta := &ves_io_schema.ObjectReplaceMetaType{}
	updateSpec := &ves_io_schema_bigip_irule.ReplaceSpecType{}
	updateReq := &ves_io_schema_bigip_irule.ReplaceRequest{
		Metadata: updateMeta,
		Spec:     updateSpec,
	}

	if v, ok := d.GetOk("annotations"); ok && !isIntfNil(v) {

		ms := map[string]string{}

		for k, v := range v.(map[string]interface{}) {
			val := v.(string)
			ms[k] = val
		}
		updateMeta.Annotations = ms
	}

	if v, ok := d.GetOk("description"); ok && !isIntfNil(v) {
		updateMeta.Description =
			v.(string)
	}

	if v, ok := d.GetOk("disable"); ok && !isIntfNil(v) {
		updateMeta.Disable =
			v.(bool)
	}

	if v, ok := d.GetOk("labels"); ok && !isIntfNil(v) {

		ms := map[string]string{}

		for k, v := range v.(map[string]interface{}) {
			val := v.(string)
			ms[k] = val
		}
		updateMeta.Labels = ms
	}

	if v, ok := d.GetOk("name"); ok && !isIntfNil(v) {
		updateMeta.Name =
			v.(string)
	}

	if v, ok := d.GetOk("namespace"); ok && !isIntfNil(v) {
		updateMeta.Namespace =
			v.(string)
	}

	if v, ok := d.GetOk("code"); ok && !isIntfNil(v) {

		updateSpec.Code =
			v.(string)

	}

	if v, ok := d.GetOk("irule_name"); ok && !isIntfNil(v) {

		updateSpec.IruleName =
			v.(string)

	}

	if v, ok := d.GetOk("source"); ok && !isIntfNil(v) {

		updateSpec.Source =
			v.(string)

	}

	log.Printf("[DEBUG] Updating Volterra BigipIrule obj with struct: %+v", updateReq)

	err := client.ReplaceObject(context.Background(), ves_io_schema_bigip_irule.ObjectType, updateReq)
	if err != nil {
		return fmt.Errorf("error updating BigipIrule: %s", err)
	}

	return resourceVolterraBigipIruleRead(d, meta)
}

func resourceVolterraBigipIruleDelete(d *schema.ResourceData, meta interface{}) error {
	client := meta.(*APIClient)
	name := d.Get("name").(string)
	namespace := d.Get("namespace").(string)

	_, err := client.GetObject(context.Background(), ves_io_schema_bigip_irule.ObjectType, namespace, name)
	if err != nil {
		if strings.Contains(err.Error(), "status code 404") {
			log.Printf("[INFO] BigipIrule %s no longer exists", d.Id())
			d.SetId("")
			return nil
		}
		return fmt.Errorf("Error finding Volterra BigipIrule before deleting %q: %s", d.Id(), err)
	}

	log.Printf("[DEBUG] Deleting Volterra BigipIrule obj with name %+v in namespace %+v", name, namespace)
	opts := []vesapi.CallOpt{
		vesapi.WithFailIfReferred(),
	}
	return client.DeleteObject(context.Background(), ves_io_schema_bigip_irule.ObjectType, namespace, name, opts...)
}
