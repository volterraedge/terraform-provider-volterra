//
// Copyright (c) 2026 Volterra, Inc. All rights reserved.
// Code generated by ves-gen-tf-provider. DO NOT EDIT.
//

package volterra

import (
	"context"
	"fmt"
	"log"
	"strings"

	"github.com/hashicorp/terraform-plugin-sdk/v2/helper/schema"

	"gopkg.volterra.us/stdlib/client/vesapi"

	ves_io_schema "github.com/volterraedge/terraform-provider-volterra/pbgo/extschema/schema"
	ves_io_schema_authorization_server "github.com/volterraedge/terraform-provider-volterra/pbgo/extschema/schema/authorization_server"
)

// resourceVolterraAuthorizationServer is implementation of Volterra's AuthorizationServer resources
func resourceVolterraAuthorizationServer() *schema.Resource {
	return &schema.Resource{
		Create: resourceVolterraAuthorizationServerCreate,
		Read:   resourceVolterraAuthorizationServerRead,
		Update: resourceVolterraAuthorizationServerUpdate,
		Delete: resourceVolterraAuthorizationServerDelete,

		Schema: map[string]*schema.Schema{

			"annotations": {
				Type:     schema.TypeMap,
				Optional: true,
			},

			"description": {
				Type:     schema.TypeString,
				Optional: true,
			},

			"disable": {
				Type:     schema.TypeBool,
				Optional: true,
			},

			"labels": {
				Type:     schema.TypeMap,
				Optional: true,
			},

			"name": {
				Type:     schema.TypeString,
				Required: true,
				ForceNew: true,
			},

			"namespace": {
				Type:     schema.TypeString,
				Required: true,
				ForceNew: true,
			},

			"jwks_uri": {
				Type:     schema.TypeString,
				Required: true,
			},
		},
	}
}

// resourceVolterraAuthorizationServerCreate creates AuthorizationServer resource
func resourceVolterraAuthorizationServerCreate(d *schema.ResourceData, meta interface{}) error {
	client := meta.(*APIClient)

	createMeta := &ves_io_schema.ObjectCreateMetaType{}
	createSpec := &ves_io_schema_authorization_server.CreateSpecType{}
	createReq := &ves_io_schema_authorization_server.CreateRequest{
		Metadata: createMeta,
		Spec:     createSpec,
	}

	if v, ok := d.GetOk("annotations"); ok && !isIntfNil(v) {

		ms := map[string]string{}

		for k, v := range v.(map[string]interface{}) {
			val := v.(string)
			ms[k] = val
		}
		createMeta.Annotations = ms
	}

	if v, ok := d.GetOk("description"); ok && !isIntfNil(v) {
		createMeta.Description =
			v.(string)
	}

	if v, ok := d.GetOk("disable"); ok && !isIntfNil(v) {
		createMeta.Disable =
			v.(bool)
	}

	if v, ok := d.GetOk("labels"); ok && !isIntfNil(v) {

		ms := map[string]string{}

		for k, v := range v.(map[string]interface{}) {
			val := v.(string)
			ms[k] = val
		}
		createMeta.Labels = ms
	}

	if v, ok := d.GetOk("name"); ok && !isIntfNil(v) {
		createMeta.Name =
			v.(string)
	}

	if v, ok := d.GetOk("namespace"); ok && !isIntfNil(v) {
		createMeta.Namespace =
			v.(string)
	}

	//jwks_uri
	if v, ok := d.GetOk("jwks_uri"); ok && !isIntfNil(v) {

		createSpec.JwksUri =
			v.(string)

	}

	log.Printf("[DEBUG] Creating Volterra AuthorizationServer object with struct: %+v", createReq)

	createAuthorizationServerResp, err := client.CreateObject(context.Background(), ves_io_schema_authorization_server.ObjectType, createReq)
	if err != nil {
		return fmt.Errorf("error creating AuthorizationServer: %s", err)
	}
	d.SetId(createAuthorizationServerResp.GetObjSystemMetadata().GetUid())

	return resourceVolterraAuthorizationServerRead(d, meta)
}

func resourceVolterraAuthorizationServerRead(d *schema.ResourceData, meta interface{}) error {
	client := meta.(*APIClient)
	name := d.Get("name").(string)
	namespace := d.Get("namespace").(string)

	resp, err := client.GetObject(context.Background(), ves_io_schema_authorization_server.ObjectType, namespace, name)
	if err != nil {
		if strings.Contains(err.Error(), "status code 404") {
			log.Printf("[INFO] AuthorizationServer %s no longer exists", d.Id())
			d.SetId("")
			return nil
		}
		return fmt.Errorf("Error finding Volterra AuthorizationServer %q: %s", d.Id(), err)
	}
	return setAuthorizationServerFields(client, d, resp)
}

func setAuthorizationServerFields(client *APIClient, d *schema.ResourceData, resp vesapi.GetObjectResponse) error {
	metadata := resp.GetObjMetadata()

	d.Set("annotations", metadata.GetAnnotations())

	d.Set("description", metadata.GetDescription())

	d.Set("disable", metadata.GetDisable())

	d.Set("labels", metadata.GetLabels())

	d.Set("name", metadata.GetName())

	d.Set("namespace", metadata.GetNamespace())

	return nil
}

// resourceVolterraAuthorizationServerUpdate updates AuthorizationServer resource
func resourceVolterraAuthorizationServerUpdate(d *schema.ResourceData, meta interface{}) error {
	client := meta.(*APIClient)

	updateMeta := &ves_io_schema.ObjectReplaceMetaType{}
	updateSpec := &ves_io_schema_authorization_server.ReplaceSpecType{}
	updateReq := &ves_io_schema_authorization_server.ReplaceRequest{
		Metadata: updateMeta,
		Spec:     updateSpec,
	}

	if v, ok := d.GetOk("annotations"); ok && !isIntfNil(v) {

		ms := map[string]string{}

		for k, v := range v.(map[string]interface{}) {
			val := v.(string)
			ms[k] = val
		}
		updateMeta.Annotations = ms
	}

	if v, ok := d.GetOk("description"); ok && !isIntfNil(v) {
		updateMeta.Description =
			v.(string)
	}

	if v, ok := d.GetOk("disable"); ok && !isIntfNil(v) {
		updateMeta.Disable =
			v.(bool)
	}

	if v, ok := d.GetOk("labels"); ok && !isIntfNil(v) {

		ms := map[string]string{}

		for k, v := range v.(map[string]interface{}) {
			val := v.(string)
			ms[k] = val
		}
		updateMeta.Labels = ms
	}

	if v, ok := d.GetOk("name"); ok && !isIntfNil(v) {
		updateMeta.Name =
			v.(string)
	}

	if v, ok := d.GetOk("namespace"); ok && !isIntfNil(v) {
		updateMeta.Namespace =
			v.(string)
	}

	if v, ok := d.GetOk("jwks_uri"); ok && !isIntfNil(v) {

		updateSpec.JwksUri =
			v.(string)

	}

	log.Printf("[DEBUG] Updating Volterra AuthorizationServer obj with struct: %+v", updateReq)

	err := client.ReplaceObject(context.Background(), ves_io_schema_authorization_server.ObjectType, updateReq)
	if err != nil {
		return fmt.Errorf("error updating AuthorizationServer: %s", err)
	}

	return resourceVolterraAuthorizationServerRead(d, meta)
}

func resourceVolterraAuthorizationServerDelete(d *schema.ResourceData, meta interface{}) error {
	client := meta.(*APIClient)
	name := d.Get("name").(string)
	namespace := d.Get("namespace").(string)

	_, err := client.GetObject(context.Background(), ves_io_schema_authorization_server.ObjectType, namespace, name)
	if err != nil {
		if strings.Contains(err.Error(), "status code 404") {
			log.Printf("[INFO] AuthorizationServer %s no longer exists", d.Id())
			d.SetId("")
			return nil
		}
		return fmt.Errorf("Error finding Volterra AuthorizationServer before deleting %q: %s", d.Id(), err)
	}

	log.Printf("[DEBUG] Deleting Volterra AuthorizationServer obj with name %+v in namespace %+v", name, namespace)
	opts := []vesapi.CallOpt{
		vesapi.WithFailIfReferred(),
	}
	return client.DeleteObject(context.Background(), ves_io_schema_authorization_server.ObjectType, namespace, name, opts...)
}
