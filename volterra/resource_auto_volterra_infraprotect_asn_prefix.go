//
// Copyright (c) 2018 Volterra, Inc. All rights reserved.
// Code generated by ves-gen-tf-provider. DO NOT EDIT.
//

package volterra

import (
	"context"
	"fmt"
	"log"
	"strings"

	"github.com/hashicorp/terraform-plugin-sdk/v2/helper/schema"
	"gopkg.volterra.us/stdlib/client/vesapi"

	ves_io_schema "github.com/volterraedge/terraform-provider-volterra/pbgo/extschema/schema"
	ves_io_schema_infraprotect_asn_prefix "github.com/volterraedge/terraform-provider-volterra/pbgo/extschema/schema/infraprotect_asn_prefix"
	ves_io_schema_views "github.com/volterraedge/terraform-provider-volterra/pbgo/extschema/schema/views"
)

// resourceVolterraInfraprotectAsnPrefix is implementation of Volterra's InfraprotectAsnPrefix resources
func resourceVolterraInfraprotectAsnPrefix() *schema.Resource {
	return &schema.Resource{
		Create: resourceVolterraInfraprotectAsnPrefixCreate,
		Read:   resourceVolterraInfraprotectAsnPrefixRead,
		Update: resourceVolterraInfraprotectAsnPrefixUpdate,
		Delete: resourceVolterraInfraprotectAsnPrefixDelete,

		Schema: map[string]*schema.Schema{

			"annotations": {
				Type:     schema.TypeMap,
				Optional: true,
			},

			"description": {
				Type:     schema.TypeString,
				Optional: true,
			},

			"disable": {
				Type:     schema.TypeBool,
				Optional: true,
			},

			"labels": {
				Type:     schema.TypeMap,
				Optional: true,
			},

			"name": {
				Type:     schema.TypeString,
				Required: true,
				ForceNew: true,
			},

			"namespace": {
				Type:     schema.TypeString,
				Required: true,
				ForceNew: true,
			},

			"asn": {

				Type:     schema.TypeList,
				MaxItems: 1,
				Required: true,
				Elem: &schema.Resource{
					Schema: map[string]*schema.Schema{

						"name": {
							Type:     schema.TypeString,
							Optional: true,
						},
						"namespace": {
							Type:     schema.TypeString,
							Optional: true,
						},
						"tenant": {
							Type:     schema.TypeString,
							Optional: true,
						},
					},
				},
			},

			"prefix": {
				Type:     schema.TypeString,
				Required: true,
			},
		},
	}
}

// resourceVolterraInfraprotectAsnPrefixCreate creates InfraprotectAsnPrefix resource
func resourceVolterraInfraprotectAsnPrefixCreate(d *schema.ResourceData, meta interface{}) error {
	client := meta.(*APIClient)

	createMeta := &ves_io_schema.ObjectCreateMetaType{}
	createSpec := &ves_io_schema_infraprotect_asn_prefix.CreateSpecType{}
	createReq := &ves_io_schema_infraprotect_asn_prefix.CreateRequest{
		Metadata: createMeta,
		Spec:     createSpec,
	}

	if v, ok := d.GetOk("annotations"); ok && !isIntfNil(v) {

		ms := map[string]string{}

		for k, v := range v.(map[string]interface{}) {
			val := v.(string)
			ms[k] = val
		}
		createMeta.Annotations = ms
	}

	if v, ok := d.GetOk("description"); ok && !isIntfNil(v) {
		createMeta.Description =
			v.(string)
	}

	if v, ok := d.GetOk("disable"); ok && !isIntfNil(v) {
		createMeta.Disable =
			v.(bool)
	}

	if v, ok := d.GetOk("labels"); ok && !isIntfNil(v) {

		ms := map[string]string{}

		for k, v := range v.(map[string]interface{}) {
			val := v.(string)
			ms[k] = val
		}
		createMeta.Labels = ms
	}

	if v, ok := d.GetOk("name"); ok && !isIntfNil(v) {
		createMeta.Name =
			v.(string)
	}

	if v, ok := d.GetOk("namespace"); ok && !isIntfNil(v) {
		createMeta.Namespace =
			v.(string)
	}

	//asn
	if v, ok := d.GetOk("asn"); ok && !isIntfNil(v) {

		sl := v.([]interface{})
		asnInt := &ves_io_schema_views.ObjectRefType{}
		createSpec.Asn = asnInt

		for _, set := range sl {
			if set != nil {
				aMapToStrVal := set.(map[string]interface{})
				if val, ok := aMapToStrVal["name"]; ok && !isIntfNil(v) {
					asnInt.Name = val.(string)
				}
				if val, ok := aMapToStrVal["namespace"]; ok && !isIntfNil(v) {
					asnInt.Namespace = val.(string)
				}

				if val, ok := aMapToStrVal["tenant"]; ok && !isIntfNil(v) {
					asnInt.Tenant = val.(string)
				}
			}
		}

	}

	//prefix
	if v, ok := d.GetOk("prefix"); ok && !isIntfNil(v) {

		createSpec.Prefix =
			v.(string)

	}

	log.Printf("[DEBUG] Creating Volterra InfraprotectAsnPrefix object with struct: %+v", createReq)

	createInfraprotectAsnPrefixResp, err := client.CreateObject(context.Background(), ves_io_schema_infraprotect_asn_prefix.ObjectType, createReq)
	if err != nil {
		return fmt.Errorf("error creating InfraprotectAsnPrefix: %s", err)
	}
	d.SetId(createInfraprotectAsnPrefixResp.GetObjSystemMetadata().GetUid())

	return resourceVolterraInfraprotectAsnPrefixRead(d, meta)
}

func resourceVolterraInfraprotectAsnPrefixRead(d *schema.ResourceData, meta interface{}) error {
	client := meta.(*APIClient)
	name := d.Get("name").(string)
	namespace := d.Get("namespace").(string)

	resp, err := client.GetObject(context.Background(), ves_io_schema_infraprotect_asn_prefix.ObjectType, namespace, name)
	if err != nil {
		if strings.Contains(err.Error(), "status code 404") {
			log.Printf("[INFO] InfraprotectAsnPrefix %s no longer exists", d.Id())
			d.SetId("")
			return nil
		}
		return fmt.Errorf("Error finding Volterra InfraprotectAsnPrefix %q: %s", d.Id(), err)
	}
	return setInfraprotectAsnPrefixFields(client, d, resp)
}

func setInfraprotectAsnPrefixFields(client *APIClient, d *schema.ResourceData, resp vesapi.GetObjectResponse) error {
	metadata := resp.GetObjMetadata()

	d.Set("annotations", metadata.GetAnnotations())

	d.Set("description", metadata.GetDescription())

	d.Set("disable", metadata.GetDisable())

	d.Set("labels", metadata.GetLabels())

	d.Set("name", metadata.GetName())

	d.Set("namespace", metadata.GetNamespace())

	return nil
}

// resourceVolterraInfraprotectAsnPrefixUpdate updates InfraprotectAsnPrefix resource
func resourceVolterraInfraprotectAsnPrefixUpdate(d *schema.ResourceData, meta interface{}) error {
	client := meta.(*APIClient)

	updateMeta := &ves_io_schema.ObjectReplaceMetaType{}
	updateSpec := &ves_io_schema_infraprotect_asn_prefix.ReplaceSpecType{}
	updateReq := &ves_io_schema_infraprotect_asn_prefix.ReplaceRequest{
		Metadata: updateMeta,
		Spec:     updateSpec,
	}

	if v, ok := d.GetOk("annotations"); ok && !isIntfNil(v) {

		ms := map[string]string{}

		for k, v := range v.(map[string]interface{}) {
			val := v.(string)
			ms[k] = val
		}
		updateMeta.Annotations = ms
	}

	if v, ok := d.GetOk("description"); ok && !isIntfNil(v) {
		updateMeta.Description =
			v.(string)
	}

	if v, ok := d.GetOk("disable"); ok && !isIntfNil(v) {
		updateMeta.Disable =
			v.(bool)
	}

	if v, ok := d.GetOk("labels"); ok && !isIntfNil(v) {

		ms := map[string]string{}

		for k, v := range v.(map[string]interface{}) {
			val := v.(string)
			ms[k] = val
		}
		updateMeta.Labels = ms
	}

	if v, ok := d.GetOk("name"); ok && !isIntfNil(v) {
		updateMeta.Name =
			v.(string)
	}

	if v, ok := d.GetOk("namespace"); ok && !isIntfNil(v) {
		updateMeta.Namespace =
			v.(string)
	}

	log.Printf("[DEBUG] Updating Volterra InfraprotectAsnPrefix obj with struct: %+v", updateReq)

	err := client.ReplaceObject(context.Background(), ves_io_schema_infraprotect_asn_prefix.ObjectType, updateReq)
	if err != nil {
		return fmt.Errorf("error updating InfraprotectAsnPrefix: %s", err)
	}

	return resourceVolterraInfraprotectAsnPrefixRead(d, meta)
}

func resourceVolterraInfraprotectAsnPrefixDelete(d *schema.ResourceData, meta interface{}) error {
	client := meta.(*APIClient)
	name := d.Get("name").(string)
	namespace := d.Get("namespace").(string)

	_, err := client.GetObject(context.Background(), ves_io_schema_infraprotect_asn_prefix.ObjectType, namespace, name)
	if err != nil {
		if strings.Contains(err.Error(), "status code 404") {
			log.Printf("[INFO] InfraprotectAsnPrefix %s no longer exists", d.Id())
			d.SetId("")
			return nil
		}
		return fmt.Errorf("Error finding Volterra InfraprotectAsnPrefix before deleting %q: %s", d.Id(), err)
	}

	log.Printf("[DEBUG] Deleting Volterra InfraprotectAsnPrefix obj with name %+v in namespace %+v", name, namespace)
	opts := []vesapi.CallOpt{
		vesapi.WithFailIfReferred(),
	}
	return client.DeleteObject(context.Background(), ves_io_schema_infraprotect_asn_prefix.ObjectType, namespace, name, opts...)
}
